name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Código
        uses: actions/checkout@v4

      - name: Compilar APK usando Docker Oficial do Buildozer
        run: |
          # Cria a pasta .buildozer localmente para o mapeamento do volume
          mkdir -p .buildozer
          
          # Executa o contentor oficial com as permissões corretas de escrita
          docker run --rm \
            -v ${{ github.workspace }}:/home/user/hostdir \
            -v ${{ github.workspace }}/.buildozer:/home/user/.buildozer \
            kivy/buildozer:latest \
            android debug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: GestorDeVoo-APK
          path: bin/*.apk
